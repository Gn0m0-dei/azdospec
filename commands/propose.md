---
description: Draft a change as Azure Boards work items, and create them once approved
argument-hint: "<what you want to build>"
disable-model-invocation: true
---

Follow the procedure in `${CLAUDE_PLUGIN_ROOT}/skills/azdospec/references/propose.md`,
using the conventions in `${CLAUDE_PLUGIN_ROOT}/skills/azdospec/references/work-items.md`.

Nothing is created in Azure DevOps until the user approves the draft.

The change to propose: $ARGUMENTS
